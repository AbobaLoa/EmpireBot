[package]
name = "empirebot-desktop"
version = "1.1.0"
description = "EmpireBot Tauri desktop shell"
authors = ["AbobaLoa"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
