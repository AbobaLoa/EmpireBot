{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window can talk to the Python engine",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
